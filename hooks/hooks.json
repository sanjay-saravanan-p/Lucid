{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat ${CLAUDE_PLUGIN_ROOT}/ascii-banner.txt 2>/dev/null || type ${CLAUDE_PLUGIN_ROOT}\\ascii-banner.txt 2>nul || true"
          }
        ]
      }
    ]
  }
}
